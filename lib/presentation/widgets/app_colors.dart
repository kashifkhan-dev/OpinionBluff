import 'package:flutter/material.dart';

class AppColors {
  final Color card = Colors.white.withAlpha(20);
  final Color border = Colors.white.withAlpha(30);
  final Color surface = Colors.white.withAlpha(10);
  final Color textPrimary = Colors.white;
  final Color primary = const Color(0xFFFF3B30);
  final Color accent = const Color(0xFF007AFF);
  final Color progress = const Color(0xFF34C759); // iOS-style premium green
}
